#!/usr/bin/sh

serve() {
  cd /home/cprussin/Projects/ui/Sources
  npm run-script sysinfo_serve
}

show() {
  /usr/bin/chromium --profile-directory=Default --app-id=aodpdhndbklbinfncfhcammdhfnjbmjo &
}

case $1 in
  serve) serve ;;
  show) show ;;
  *) show && serve ;;
esac
